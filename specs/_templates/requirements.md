# Requirements — <Feature Name>

- **Status:** Draft
- **Spec ID:** NNN
- **Author:**
- **Last updated:**

## 1. Summary

<1–3 sentences: what this feature is and the value it delivers.>

## 2. Goals

- <measurable goal>

## 3. Non-goals

- <explicitly out of scope>

## 4. User stories & acceptance criteria

Use EARS notation (see specs/README.md). Number every requirement so tasks can
reference it.

### R1 — <story title>

> As a `<role>`, I want `<capability>`, so that `<benefit>`.

- **R1.1** WHEN `<trigger>`, the system SHALL `<response>`.
- **R1.2** IF `<bad condition>`, THEN the system SHALL `<safe response>`.

## 5. Constraints & assumptions

- <constraint>

## 6. Open questions

- [ ] <question to resolve before Approved>
